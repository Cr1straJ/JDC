@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Подтверждение электронной почты";
}

<h1>@ViewData["Title"]</h1>

<p>Пожалуйста, проверьте свою электронную почту для подтверждения учетной записи.</p>
<hr />
    
<div class="row">
    <div class="col-md-4">
        <form asp-action="RegisterConfirmation" method="post">
            <input type="hidden" asp-for="Id" />            
            <div asp-validation-summary="All" class="text-danger"></div>                
            <div class="form-group @(Model.Id != -1 ? "d-none" : "")">
                <label asp-for="Email" class="other_info"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>            

            <div class="form-group">
                <label asp-for="ConfirmationCode" class="other_info"></label>
                <input asp-for="ConfirmationCode" class="form-control input_number"/>
                <span asp-validation-for="ConfirmationCode" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">Подтвердить</button>
        </form>
    </div>
</div>
